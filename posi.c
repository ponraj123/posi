#include <stdio.h>

int main(void) {
	int n;
	scanf("%d",&n);
	if(n>0)
	{
		printf("The number is positive no:");
	}
	else if(n<0)
	{
		printf("The number is nagative no:");
	}
	else
	{
		printf("The number is equal to zero:");
	}
	// your code goes here
	return 0;
}
